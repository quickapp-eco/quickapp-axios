{
  "name": "example",
  "version": "1.0.1",
  "subversion": {
    "toolkit": "0.6.6"
  },
  "description": "",
  "scripts": {
    "build": "hap build",
    "server": "hap server --clear-records",
    "watch": "hap watch --match-sourcemap",
    "start": "hap server & hap watch --devtool=source-map"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^5.12.1",
    "eslint-plugin-hybrid": "0.0.5",
    "hap-toolkit": "^0.6.6"
  }
}
